# ParQuiz (Dojo)

Initial monorepo scaffold for the ParQuiz project.

## Directories

- `frontend/src/api/`: API client layer for on-chain calls (runs with `bun run dev` in frontend).
- `PARQUIZ_DOJO_SPEC.md`: v2 functional and technical specification.
